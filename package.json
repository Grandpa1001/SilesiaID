{
  "name": "silesiaid",
  "private": true,
  "description": "Monorepo root — tylko pod deploy (Railway/Railpack). Backend: folder backend/.",
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "node backend/dist/index.js"
  }
}
